class calculator
{
  public:
    int multiply(int x, int y);
    int add(int x, int y);
 };
int calculator::multiply(int x, int y)
{
  return x*y;
}
int calculator::add(int x, int y)
{
  return x+y;
}
